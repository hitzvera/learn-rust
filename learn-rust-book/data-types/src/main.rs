mod exercise;

use exercise::one::exercise_number_one;
use exercise::three::exercise_number_three;
use exercise::two::exercise_number_two;

fn main() {
    // exercise_number_one();
    // exercise_number_two();
    exercise_number_three();
}
